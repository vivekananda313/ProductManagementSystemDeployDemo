apiVersion: v1
kind: Service

metadata:
  name: product-management

spec:
  type: NodePort

  selector:
    app: product-management

  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082